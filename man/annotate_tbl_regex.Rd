% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations_utils.R
\name{annotate_tbl_regex}
\alias{annotate_tbl_regex}
\title{Annotate a Duckplyr Table with Regex Match Flags}
\usage{
annotate_tbl_regex(
  tbl_data,
  query_col,
  patterns,
  filter_out = FALSE,
  name_type = c("index", "pattern")
)
}
\arguments{
\item{tbl_data}{A duckplyr table (e.g. \code{idata$receptors}).}

\item{query_col}{Character(1). Name of the sequence column to search.}

\item{patterns}{Character vector of regular expressions.}

\item{filter_out}{Logical(1). If TRUE, filter out sequences which don't match the pattern.}

\item{name_type}{One of \code{"pattern"} or \code{"index"}:
- \code{"pattern"}: column names \verb{match_regex_<sanitized_pattern>}
- \code{"index"}:   column names `match_regex_\if{html}{\out{<i>}}}
}
\value{
The input table with new logical match columns added.
}
\description{
Adds one logical “does this sequence match this regex?” column per pattern,
using DuckDB’s \code{regexp_matches()} via the \code{dd} escape hatch.
}
